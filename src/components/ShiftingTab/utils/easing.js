import {
  Easing,
} from 'react-native'

export const easeInOut = new Easing.bezier(0.4, 0.0, 0.2, 1) // eslint-disable-line
export const easeOut = new Easing.bezier(0, 0, 0.2, 1)  // eslint-disable-line
